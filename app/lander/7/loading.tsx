/** Zichtbaar alleen als de server-redirect even duurt. */
export default function Lander7Loading() {
  return (
    <div
      style={{
        minHeight: "100vh",
        margin: 0,
        background: "#f5f0ff",
      }}
    />
  );
}
