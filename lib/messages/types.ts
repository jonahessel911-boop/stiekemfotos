export type Messages = {
  nav: {
    feed: string;
    messages: string;
    profiles: string;
    credits: string;
  };
  common: {
    notifications: string;
    logout: string;
    online: string;
  };
};

