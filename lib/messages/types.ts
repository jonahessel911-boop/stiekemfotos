export type Messages = {
  nav: {
    feed: string;
    messages: string;
    profiles: string;
    gallery: string;
    credits: string;
  };
  common: {
    notifications: string;
    logout: string;
    online: string;
  };
};

