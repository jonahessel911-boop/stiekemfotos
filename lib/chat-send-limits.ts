/** Max. tekens per gebruikersbericht (tekstdeel; foto-only telt niet mee). */
export const MAX_USER_MESSAGE_CHARS = 300;

/** Max. berichten in één gebundelde batch vóór verzenden naar de server. */
export const MAX_OUTGOING_BATCH_SIZE = 4;
